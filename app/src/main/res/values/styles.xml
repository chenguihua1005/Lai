<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mytoolbar_green</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Dialog.Alert" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowNoTitle">false</item>
    </style>

    <style name="dialog" parent="@style/Theme.AppCompat.Dialog">
        <!-- 设置未浮动窗口 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 设置无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置完全透明 -->
        <item name="android:windowBackground">@color/sc_transparent_background</item>
        <!-- 设置屏幕变暗 -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>


    <style name="mypopwindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/popshow_anim</item>
        <!-- 指定显示的动画xml -->

        <item name="android:windowExitAnimation">@anim/pophidden_anim</item>
        <!-- 指定消失的动画xml -->
    </style>

    <style name="customCheckBoxTheme" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/check_box</item>
    </style>

    <!--指定tabLayout样式-->
    <style name="myTabLayout" parent="Widget.Design.TabLayout">
        <!--<item name="tabMaxWidth">36dp</item> --><!--TAB最大宽度-->
        <item name="tabIndicatorColor">#00000000</item> <!--TAB底部滑块颜色-->
        <item name="tabIndicatorHeight">0dp</item> <!--TAB底部滑块高度-->
        <item name="tabPaddingStart">5dp</item> <!--TAB左边padding-->
        <item name="tabPaddingEnd">5dp</item> <!--TAB右边padding-->
        <item name="tabBackground">@null</item> <!--TAB背景色-->
        <item name="tabTextAppearance">@style/MyCustomTabTextAppearance</item> <!--TAB文字样式-->
        <!--<item name="tabSelectedTextColor">?android:textColorPrimary</item> &lt;!&ndash;TAB选中文字颜色&ndash;&gt;-->
    </style>
    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab"> <!--TAB文字样式-->
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">?android:textColorSecondary</item>
        <item name="textAllCaps">false</item> <!--英文大写,默认true-->
    </style>
</resources>
